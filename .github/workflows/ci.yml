name: CI (Quick)

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Install prek
        run: pip install prek==0.3.3

      - name: Run pre-commit hooks
        run: prek run --all-files

      - name: Run quick tests
        run: DONBURI_TEST_LEVEL=quick bash test/test_donburi.sh
