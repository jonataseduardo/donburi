name: CI (Full)

on:
  push:
    branches: [dev]
  pull_request:
    branches: [main]

jobs:
  full-test:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Run full tests
        run: bash test/test_donburi.sh
